# 文献雷达 API — HTTPS 终止于 Caddy，反代到 literature-api:8000
# LITERATURE_DOMAIN、ACME_EMAIL 来自 deploy/https.env（见 deploy/https.env.example）

{
	email {$ACME_EMAIL}
}

{$LITERATURE_DOMAIN} {
	encode gzip zstd
	reverse_proxy literature-api:8000
}
