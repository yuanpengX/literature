# 文献雷达 API — HTTPS 终止于 Caddy，反代到 literature-api:8000
# LITERATURE_DOMAIN 来自 deploy/https.env（见 README）
# 不在此写全局 email，避免 ACME_EMAIL 为空时 Caddy 解析失败；Let's Encrypt 仍可正常签发。

{$LITERATURE_DOMAIN} {
	encode gzip zstd
	reverse_proxy literature-api:8000
}
