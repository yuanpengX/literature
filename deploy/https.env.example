# 可选：复制为 deploy/https.env（已 gitignore）用于覆盖 deploy/https.defaults.env
#
# 默认已由 https.defaults.env 提供 cppteam.cn + fullchain.pem/privkey.pem。
# 仅当与默认不同时取消注释，例如阿里云证书在 /ssl 且文件名为 cert.pem / cert.key 时：
#
# LITERATURE_DOMAIN=cppteam.cn
# CADDY_TLS_CERT=/etc/caddy/certs/cert.pem
# CADDY_TLS_KEY=/etc/caddy/certs/cert.key
