# 复制为 deploy/https.env 后填写（deploy/https.env 已 gitignore，勿提交）
# 域名须已解析到本机公网 IP，且防火墙放行 80、443（Let’s Encrypt HTTP-01）

# 仅域名，无协议、无路径、无端口。例：api.example.com
LITERATURE_DOMAIN=

# Let’s Encrypt 账户邮箱（Caddy 全局 email，用于证书到期提醒等）
ACME_EMAIL=
