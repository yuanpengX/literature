# 仅用于 docker compose 解析（卷挂载）。启动命令：
# docker compose --env-file deploy/compose.https.env -f docker-compose.https-stack.yml up -d
# 不配时 compose 内默认仍为 ./deploy/certs

# 宿主机证书目录（内含 fullchain.pem/privkey.pem，或阿里云 cert.pem/cert.key）
CERT_HOST_DIR=./deploy/certs
