---
description: 需求交付后必做 code review、git 提交推送与 Docker 重建
alwaysApply: true
---

# 文献雷达（literature）交付工作流

在**完成一项新需求或一组相关改动**后，除非用户明确说「不要提交 / 不要部署」，否则按顺序执行：

## 1. Code review

- 对照需求自查：边界情况、错误处理、与现有 Android / 小程序 / server 约定是否一致。
- 快速扫一眼 diff：无无关改动、无调试残留、无密钥进仓库。
- 如有测试命令（如 `./gradlew :app:compileDebugKotlin`、`python -m compileall`），在合理范围内跑通。

## 2. Commit

- 在 **literature 仓库根目录**执行 `git status`，只提交与需求相关的文件。
- 使用清晰的中文或英文提交说明（完整句子，说明「做了什么、为什么」）。

## 3. Push

- `git push` 到当前分支的默认远程（如 `origin`）。

## 4. Docker 重新 build / 部署

本仓库常用 **HTTPS 栈**（根目录 `docker-compose.https-stack.yml`）。服务端代码或 `server/Dockerfile` / 依赖变更后，在**仓库根目录**执行：

```bash
docker compose -f docker-compose.https-stack.yml up -d --build
```

- 若仅改 `server/.env`，通常需 **`--force-recreate literature-api`** 或上述 `--build` 后确保 API 容器已用新镜像/环境重启。
- 若公网仍 **502** 且 Caddy 日志为 `connection refused` 到 API：多为 API 刚重启未监听；稍等或执行 **`docker compose -f docker-compose.https-stack.yml restart caddy`**。
- 若使用仅 HTTP 的 `docker-compose.yml`，则对该文件执行等价的 `up -d --build`。

**注意**：不要在规则或提交中写入真实 `.env` 密钥；Docker 说明仅涉及命令与文件名。
